name: concurrent-db-access
version: 0.1.0

targets:
  concurrent-db-access:
    main: src/concurrent-db-access.cr

crystal: 1.7.2

license: MIT

dependencies:
  dotenv:
    github: dext/crystal-dotenv
  pg:
    github: will/crystal-pg
  sqlite3:
    github: crystal-lang/crystal-sqlite3
  mysql:
    github: crystal-lang/crystal-mysql
